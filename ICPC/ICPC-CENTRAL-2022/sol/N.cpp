#include <bits/stdc++.h>

#include <iomanip>
using namespace std;

#define ll long long
#define pi pair<int, int>
#define vi vector<int, int>

void solve() {
    ll n;
    cin >> n;
    double s = 1.0 / (n + 1) * 1000000000;
    cout << s << endl;
}

int main() {
    ll n;
    cin >> n;
    while (n--) solve();
}